---
title: Home 
kind: page
---

- featured=sorted_articles[0]

.hero-unit
  %h1
    =featured[:title]
  = render('_meta', :item => featured)
  %span.label.important
    featured
  %p
    =featured[:excerpt]
  %p
    %a.btn.primary.large{:href=>featured.path}
      Read more &raquo;

- rows = (featured_count - 1) % 3
- i=-1

- (0..rows-1).each do |r|
  -i+=1
  .row
    - item0 = sorted_articles[1+(i*3)]
    - item1 = sorted_articles[1+(1+(i*3))]
    - item2 = sorted_articles[1+(2+(i*3))]


    .span6
      %h2
        =item0[:title]
      .meta
        =flag_image(item0[:lang])
        =pretty_time(item0[:created_at])
      %p
        =item0[:excerpt]
      %p
        %a.btn{:href=>item0.path}
          Read more &raquo;

    .span5
      %h2
        =item1[:title]
      .meta
        =flag_image(item1[:lang])
        =pretty_time(item1[:created_at])
      %p
        =item1[:excerpt]
      %p
        %a.btn{:href=>item1.path}
          Read more &raquo;

    .span5
      %h2
        =item2[:title]
      .meta
        =flag_image(item2[:lang])
        =pretty_time(item2[:created_at])
      %p
        =item2[:excerpt]
      %p
        %a.btn{:href=>item.path}
          Read more &raquo;
.row
  %a.btn.primary.large{:href=>'/posts.html'}
    All posts

/  = render('_article', :item => item, :is_embedded=> true, :show_lang=>true, :featured=>true)


